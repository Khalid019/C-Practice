#include <iostream>
#include <cmath>

using namespace std;

int main(){
    double x = 3.11, y = 4.11, z;
    //z= max(x, y);
    //z= min(x, y);
    //z= abs(-3);
    z= sqrt(x);
    //z= round(x);

    cout << "The result is: "<< z;
    return 0;
}